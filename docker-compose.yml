version: '3'
services:
    db:
        image: postgres
    web:
        build: .
        command: bundle exec rails s -p 3000 -b '0.0.0.0'
        volumes:
            - ./travel_code:/myapp
        ports:
            - "3000:3000"
        links:
            - db
        depends_on:
            - db
